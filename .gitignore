# .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
*.user
*.suo

# IDE / tooling
.vs/
.vscode/
.idea/
*.DotSettings.user

# NuGet
nuget.local.config
*.nupkg
*.snupkg
packages/

# OS noise
.DS_Store
Thumbs.db

# DTLS debugging output (CUPRIWEBRTC_PCAP / CUPRIWEBRTC_SSLKEYLOG). A key log holds the *live* traffic secrets of a
# real session and deprotects it completely — it must never be committed. See docs/dtls-1.3.md.
*.pcap
*.pcapng
*keylog*.log
*keylog*.txt
sslkeys*.log
