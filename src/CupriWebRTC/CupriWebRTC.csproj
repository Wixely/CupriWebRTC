<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <IsPackable>true</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="CupriWebRTC.Tests" />
  </ItemGroup>

  <ItemGroup>
    <!-- DTLS (and its crypto) via BouncyCastle — managed, the same choice CupriNet makes. -->
    <PackageReference Include="BouncyCastle.Cryptography" Version="2.7.0" />
  </ItemGroup>
</Project>
